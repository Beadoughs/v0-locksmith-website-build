import type { Metadata, Viewport } from 'next'
import { Inter, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter'
})

const geistMono = Geist_Mono({ 
  subsets: ["latin"],
  variable: '--font-geist-mono'
})

export const metadata: Metadata = {
  title: 'Melbourne Locksmiths | 24/7 Emergency Locksmith Services',
  description: 'Professional 24/7 emergency locksmith services across Melbourne. Fast response times, licensed & insured technicians. Residential, commercial & automotive locksmith solutions.',
  keywords: 'locksmith melbourne, emergency locksmith, 24/7 locksmith, car locksmith, residential locksmith, commercial locksmith, lock change, lock repair',
  openGraph: {
    title: 'Melbourne Locksmiths | 24/7 Emergency Locksmith Services',
    description: 'Professional 24/7 emergency locksmith services across Melbourne. Fast response times, licensed & insured technicians.',
    type: 'website',
    locale: 'en_AU',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  themeColor: '#1a1a2e',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${geistMono.variable} bg-background`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
